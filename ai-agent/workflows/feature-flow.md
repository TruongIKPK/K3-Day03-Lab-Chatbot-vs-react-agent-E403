# ✨ WORKFLOW: FEATURE FLOW

1. Xác định Tool mới cần thêm dựa trên sơ đồ DB 11 bảng.
2. Viết định nghĩa hàm trong `src/tools.py`.
3. Đăng ký Tool trong `AVAILABLE_TOOLS` và cập nhật `REACT_SYSTEM_PROMPT` trong `src/prompts.py`.
4. Bổ sung Test Case tương ứng vào `config/test_cases.json`.
